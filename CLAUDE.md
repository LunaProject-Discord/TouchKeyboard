# Claude Code 向け作業ルール

## 着手前に読むもの

- `docs/requirements.md` — 何を作るか
- `docs/development-plan.md` — どの順で作るか、どこで止まるか
- `docs/technical-notes.md` — 実装上の落とし穴

## 絶対制約

以下に反する実装は目的を達成できないため、いかなる理由でも採用しない。

### 1. キー送出は SendInput + KEYEVENTF_SCANCODE のみ

`WM_CHAR` / `SendKeys` / `keybd_event` は使用禁止。

通常のキーボードと同じ入力経路を通さなければ IME が介入せず、ATOK の入力ミス補正が働かない。補正が働かなければこのプロジェクトの存在意義がなくなる。

### 2. ウィンドウは WS_EX_NOACTIVATE | WS_EX_TOOLWINDOW

キーをタップするたびに入力先のフォーカスが外れる実装は仕様違反。

### 3. 画面領域の確保は SHAppBarMessage

`Topmost` で覆うだけの実装は不可。入力欄がキーボードに隠れる。

### 4. キー配列はデータとして外部化

XAML にキーを直書きしない。配列変更とレイアウト追加ができなくなる。

## 実装方針

- P/Invoke 宣言とラッパーは `Interop/` 配下に集約する。呼び出し側に散らさない
- 記憶したスキャンコードの値をそのまま埋め込まない。`docs/technical-notes.md` の値も参考値であり、実機確認で確定する
- 不確かな箇所はコメントで `// TODO: 実機確認` と明示する

## 検証について

**GUI の動作確認は開発者が実機で行う。**

- ビルドが通ったら報告し、動作確認を依頼して停止すること
- アプリを起動して「動作しました」と報告しないこと
- 各フェーズの受け入れ基準は `docs/development-plan.md` にある。基準を満たしたかどうかを判断するのは開発者であり、Claude Code ではない

## 並列実行

相互に独立した作業（Phase 0 の P/Invoke ラッパー群など）はサブエージェントで並列に進めてよい。依存関係のある作業や、同一ファイルを触る作業は逐次で行うこと。
